class SafUtil {
  Future<dynamic> pickDirectory({
    bool writePermission = false,
    bool persistablePermission = false,
  }) async {
    throw UnsupportedError('SAF is not supported on web');
  }
}
